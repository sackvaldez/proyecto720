# Variables de entorno para Caballero Solutions Power

# Puerto del servidor (Render lo asigna automáticamente)
PORT=3000

# Clave secreta para sesiones (OBLIGATORIO CAMBIAR EN PRODUCCIÓN)
# Render puede generar esto automáticamente
SESSION_SECRET=caballero-solutions-power-secret-key-change-in-production

# Contraseñas en producción (CAMBIAR OBLIGATORIAMENTE)
ADMIN_PASSWORD=tu-password-admin-seguro
=tu-password-plasman-seguro
PLASMAN2_PASSWORD=tu-password-plasman2-seguro
REHRIG_PASSWORD=tu-password-rehrig-seguro
MARTIN_PASSWORD=tu-password-martin-seguro
FAREVA_PASSWORD=tu-password-fareva-seguro
GIVAUDAN_PASSWORD=tu-password-givaudan-seguro
PHILIPS_PASSWORD=tu-password-philips-seguro
INDORAMA_PASSWORD=tu-password-indorama-seguro
STANT_PASSWORD=tu-password-stant-seguro
MARTIN2_PASSWORD=tu-password-martin2-seguro

# Entorno de ejecución
NODE_ENV=production

# Configuración de base de datos (para futuras migraciones)
# DATABASE_URL=postgresql://user:password@host:port/database

# Configuración de almacenamiento de archivos (para futuras migraciones)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_BUCKET_NAME=your_bucket_name
# AWS_REGION=us-east-1

# Base de datos (para implementación futura)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=caballero_solutions
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password

# SSL/TLS (para HTTPS en producción)
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key

# Configuración de correo (para notificaciones futuras)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Configuración de almacenamiento en la nube (para implementación futura)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_BUCKET_NAME=your-s3-bucket-name
# AWS_REGION=us-east-1